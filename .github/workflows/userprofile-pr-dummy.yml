name: userprofile-pr

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "apis/userprofile/**"
jobs:
  build:
    name: userprofile-pr-job
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required" '
